spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: base-config-server
      name: application-backstage
      profile: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.12.49:36366/eureka/
