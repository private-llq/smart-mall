spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
      name: application-file
      profile: dev
      label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka/
