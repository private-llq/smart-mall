server:
  port: 7000
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: http://47.108.155.57/arli/smart-mall2-config.git

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka/
  instance:
    instance-id: config-server:7000 #服务集群当前节点的标识
    prefer-ip-address: true #以ip的形式进行服务的注册